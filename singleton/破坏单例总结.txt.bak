破坏单例的两种方法
1. 反射
这种方法通过反射获得私有构造器，对其设置强制访问，使用构造器创建对象，破坏了单例。
解决方法：在无参构造器中判断对象是否为null，如果不为空抛出运行时异常。

2. 反序列化
将已经持久化的字节码内容，转换为IO流。通过IO流的读取，进而将读取的内容转换为Java对象，在转换过程中会重新创建对象。
解决方法：重写readResolve()方法，返回单例。当用反序列化创建对象时，会返回readResolve()执行的结果。

